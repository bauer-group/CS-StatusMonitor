# =============================================================================
# Git Attributes - Line Ending Normalization
# =============================================================================

# Auto-detect text files and normalize line endings
* text=auto

# Force LF line endings for scripts and configs (shell scripts MUST stay LF or
# they break inside the Linux container)
*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
.env* text eol=lf
Dockerfile text eol=lf
.dockerignore text eol=lf

# Markdown
*.md text
